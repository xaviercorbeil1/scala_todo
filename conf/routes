
# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET         /            controllers.TodoListController.home
GET         /todo        controllers.TodoListController.getAll
POST        /save        controllers.TodoListController.save()
POST        /done/:id        controllers.TodoListController.done(id : Int)

